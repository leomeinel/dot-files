#!/bin/bash

# Aliases
alias ssh-git='eval "$(ssh-agent -s)" && ssh-add ~/.ssh/id_ed25519_github'

alias ls='exa -la --color=automatic'
alias cat='bat --decorations *auto* --color *auto*'
alias grep='rg -s --color auto'
alias find='fd -Hs -c auto'
